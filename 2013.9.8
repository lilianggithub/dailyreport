1 数据表达方式：二进制、十进制 算法。
2 ls -al （ls   al ～、ls -al ～、ls -al -l ～）列出所有隐藏档与相关的文件属性
3 echo $LANG 显示当前语系（LANG）  
  LANG=en_us 修改语系
  echo $LANG 修改后的语系
4 date 显示当前系统时间
  date +%Y/%m/%d  只显示年月日
  date +%H：%M  只显示时间
5 cal 显示这个月的日历
  cal 2013 显示整年的月历
  cal [month][year] 显示某年某月日历
6 bc 计算器    quit退出 
7 ca[tab][tab]
  ls -al ~/.bash[tab][tab]   文件命令补齐/文件补齐
8 nano 文本编辑器
9 cd 变换目录
  pwd显示目前的目录
  mkdir 创建一个新的目录
  rmidir 删除一个空的目录
10 cp、rm、mv
11 whereis  寻找特定文件
   locate  
   which 运行档搜寻
 极重要！权限与命令间的关系
12 df 命令 区段与superblock信息查询
13 ls -l
14 df：列出文件系统的整体磁盘使用量；
   du：评估文件系统的磁盘使用量(常用在推估目录所占容量)
15 df -ahikHTm filename  
   df -h   将容量结果以易读的容量格式显示出来
   df =aT   将系统内的所有特殊文件格式及名称都列出来 
   df -h /etc  将 /etc 底下的可用的磁盘容量以易读的容量格式显示
   df -ih  将目前各个 partition 当中可用的 inode 数量列出
16 du    列出目前目录下的所有文件容量
   du [-ahskm] 文件或目录名称
   du -a 列出目前目录下的所有文件
   du -sm /*   检查根目录底下每个目录所占用的容量
17 磁盘的分割、格式化、检验与挂载
18 文件与文件系统的压缩与打包
   压缩文件的扩展名大多是：『*.tar, *.tar.gz, *.tgz, *.gz, *.Z, *.bz2』
   *.gz        gzip 程序压缩的文件；    （主要）
   *.bz2       bzip2 程序压缩的文件；   （主要）
   *.tar       tar 程序打包的数据，并没有压缩过；
   *.tar.gz    tar 程序打包的文件，其中并且经过 gzip 的压缩
   *.tar.bz2   tar 程序打包的文件，其中并且经过 bzip2 的压缩
    gzip 是应用度最广的压缩命令！目前 gzip 可以解开 compress, zip 与 gzip 等软件所压缩的文件。 至於 gzip 所创建的压缩档为 *.gz 的档名
  # gzip [-cdtv#] 档名
  # zcat 档名.gz
    选项与参数：
    -c  ：将压缩的数据输出到萤幕上，可透过数据流重导向来处理；
    -d  ：解压缩的参数；
    -t  ：可以用来检验一个压缩档的一致性～看看文件有无错误；
    -v  ：可以显示出原文件/压缩文件的压缩比等资讯；
    -#  ：压缩等级，-1 最快，但是压缩比最差、-9 最慢，但是压缩比最好！默认是 -6
19 tar 
    压　缩：tar -jcv -f filename.tar.bz2 要被压缩的文件或目录名称
    查　询：tar -jtv -f filename.tar.bz2
    解压缩：tar -jxv -f filename.tar.bz2 -C 欲解压缩的目录
20 用 dump 备份非文件系统，亦即单一目录的方法
   用 restore 观察 dump 后的备份数据内容

21*  vi 文书编辑器  
    
    一般模式：
    以 vi 打开一个档案就直接进入一般模式了(这是默认的模式)。在这个模式中， 你可以使用『上下左右』按键来移动光标，你可以使用『删除字符』或『删除整行』来处理档案内容， 也可以使用『复制、贴上』来处理你的文件数据。
    编辑模式：
    在一般模式中可以进行删除、复制、贴上等等的动作，但是却无法编辑文件内容的！ 要等到你按下『i, I, o, O, a, A, r, R』等任何一个字母之后才会进入编辑模式。注意了！通常在 Linux 中，按下这些按键时，在画面的左下方会出现『 INSERT 或 REPLACE 』的字样，此时才可以进行编辑。而如果要回到一般模式时， 则必须要按下『Esc』这个按键即可退出编辑模式。
    指令列命令模式：
    在一般模式当中，输入『 : / ? 』三个中的任何一个按钮，就可以将光标移动到最底下那一行。在这个模式当中， 可以提供你『搜寻资料』的动作，而读取、存盘、大量取代字符、离开 vi 、显示行号等等的动作则是在此模式中达成的！

    vim 具有程序编辑的能力，可以主动的以字体颜色辨别语法的正确性，方便程序设计
    
22 bash shell and shell scripts 









